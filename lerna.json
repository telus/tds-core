{
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.md", "**/*.spec.jsx", "**/*.snap", "@tds/core-selector-counter"],
      "message": "chore(packages): publish"
    }
  }
}
