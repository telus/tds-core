{
  "name": "telus-thorium-build",
  "version": "0.6.0",
  "description": "TELUS Design System",
  "license": "UNLICENSED",
  "scripts": {
    "build": "npm run build:assets && npm run build:core && npm run build:enriched && npm run build:docs",
    "build:assets": "cp core/fonts/* docs/src/html/assets/fonts/ && cp designs/thorium.sketch docs/src/html/assets/downloads/thorium.sketch",
    "build:core": "cd core && npm run build",
    "build:docs": "cd docs && npm run build",
    "build:enriched": "cd enriched && npm run build",
    "start": "concurrently --kill-others \"cd docs && npm run start\" \"cd docs && npm run devServer\"",
    "install": "npm run install:core && npm run install:enriched && npm run install:docs",
    "install:core": "cd core && npm install",
    "install:docs": "cd docs && npm install",
    "install:enriched": "cd enriched && npm install",
    "setup:links": "cd core && npm link && cd ../enriched && npm link && npm link telus-thorium-core && cd ../docs && npm link telus-thorium-core && npm link telus-thorium-enriched"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/telusdigital/telus-thorium-core.git"
  },
  "author": "TELUS Digital",
  "bugs": {
    "url": "https://github.com/telusdigital/telus-thorium-core/issues"
  },
  "devDependencies": {
    "concurrently": "^3.1.0",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.5.4"
  }
}
